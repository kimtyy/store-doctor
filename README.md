# 매장닥터 (Store Doctor)

Next.js 14 + TypeScript + Tailwind CSS + Supabase 기반 초기 프로젝트입니다.

## 시작하기

1. 종속성 설치

```bash
npm install
```

2. 개발 서버 실행

```bash
npm run dev
```

3. 환경 변수 설정

`.env.local` 파일을 생성하고 아래 값을 추가합니다.

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```
