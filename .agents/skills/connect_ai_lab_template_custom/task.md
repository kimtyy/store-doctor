- [x] 1. 구독 연장 백엔드 보완 (actions.ts)
  - [x] `extendSubscription` months 입력값 검증 (1~12 정수 아닐 시 에러)
  - [x] `subscriptions` 테이블이 없는 경우 upsert로 신규 등록 (plan='basic', status='active', 오늘+N개월)
  - [x] 연장 성공 시 `subscription_extensions` 이력 테이블에 insert 기록 남기기
- [x] 2. 구독 연장 UI 보완 (DeleteButton.tsx)
  - [x] `ExtendSubscriptionSection`에 연장 사유(reason) 입력란 추가 및 state 연동
  - [x] `customInput` 및 연장 처리 전 UI level에서 1~12 정수 범위/음수 유효성 검사 및 사유 빈값 검사 추가
  - [x] `window.confirm` 창에 사유와 함께 `"OOO님 구독을 N개월 연장합니다."` 문구 노출
- [x] 3. DB 마이그레이션 및 이력 조회 추가 (page.tsx)
  - [x] `supabase/migrations/20260719_create_subscription_extensions.sql` 파일 작성 (이미 작성 완료)
  - [x] `app/admin/page.tsx`에서 `subscription_extensions` 이력 조회 쿼리 추가 (Defensive 코딩 포함)
  - [x] `app/admin/page.tsx` 하단에 구독 연장 이력 UI 섹션 렌더링
- [x] 4. package-lock.json 동기화 및 검증
  - [x] `npm install` 실행하여 package-lock.json 파일 갱신 및 동기화
  - [x] `npm ci --dry-run`으로 의존성 정합성 검증
  - [x] `npm run build`로 최종 컴파일 테스트
- [x] 5. 완료 조건 수행
  - [x] 모든 변경사항 커밋 및 origin main에 푸시
  - [x] 커밋 해시 보고
